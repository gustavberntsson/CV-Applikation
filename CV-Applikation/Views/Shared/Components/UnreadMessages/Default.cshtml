@model int

    @if (Model > 0)
    {
        <span class="unread-count">@Model</span>
    }
<style>
    .unread-count {
        display: inline-block;
        background-color: #dc3545; 
        color: white;
        font-weight: bold;
        font-size: 1rem;
        width: 1.5rem; 
        height: 1.5rem; 
        border-radius: 50%; 
        text-align: center;
        line-height: 1.5rem; 
    }
</style>